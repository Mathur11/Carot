Description: This repo is created for POC point of view, to create the API worklows of CarotLegal website.
API workflow 1 --> carotLegalCreateContact (Login through UI and Create contact via API)
API workflow 2 --> createMatter (Login through UI and Create matter via API)

Repo Link : https://github.com/Mathur11/Carot.git

To install Playwright dependency and browsers --> 
npm init playwright@latest

To check Playwright version --> 
npx playwright --version

To run the test file --> 
npx playwright test "<FileName>"
